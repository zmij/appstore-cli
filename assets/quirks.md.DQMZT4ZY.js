import{_ as t,o as s,c as i,a2 as r}from"./chunks/framework.DBPoZGF_.js";const u=JSON.parse('{"title":"Apple quirks","description":"","frontmatter":{},"headers":[],"relativePath":"quirks.md","filePath":"quirks.md","lastUpdated":1782036012000}'),a={name:"quirks.md"};function o(n,e,l,c,p,h){return s(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="apple-quirks" tabindex="-1">Apple quirks <a class="header-anchor" href="#apple-quirks" aria-label="Permalink to &quot;Apple quirks&quot;">​</a></h1><p>Non-obvious behaviour from the App Store Connect API that the CLI works around. Each entry includes what the API does, what we do about it, and what to remember when you go beyond the CLI.</p><h2 id="_1-edit-sessions-are-listings-only" tabindex="-1">1. Edit sessions are listings-only <a class="header-anchor" href="#_1-edit-sessions-are-listings-only" aria-label="Permalink to &quot;1. Edit sessions are listings-only&quot;">​</a></h2><p>Apple&#39;s API has a <strong>session model</strong> for app metadata: you open a draft &quot;edit&quot;, make changes, then commit. The CLI threads this transparently for listings + screenshots.</p><p><strong>IAPs and subscriptions don&#39;t need an edit session.</strong> Changes apply immediately on <code>iap sync</code>. Don&#39;t try to wrap them in a session — you&#39;ll get cryptic 4xx errors.</p><p>This split mirrors how the ASC website behaves: listing edits queue up in a &quot;version under preparation&quot; you submit later; IAP/sub edits go live on save.</p><h2 id="_2-preservecurrentprice-is-the-migration-discriminator" tabindex="-1">2. <code>preserveCurrentPrice</code> is the migration discriminator <a class="header-anchor" href="#_2-preservecurrentprice-is-the-migration-discriminator" aria-label="Permalink to &quot;2. `preserveCurrentPrice` is the migration discriminator&quot;">​</a></h2><p>When you create a <code>subscriptionPrices</code> row:</p><table tabindex="0"><thead><tr><th><code>preserveCurrentPrice</code></th><th>Who sees the new price</th></tr></thead><tbody><tr><td><code>true</code> (CLI default on <code>iap sync</code>)</td><td>New subscribers only. Existing cohorts stay on their old price</td></tr><tr><td><code>false</code> (<code>iap migrate-prices --confirm</code> writes this)</td><td>Everyone — existing subscribers get notified + migrated</td></tr></tbody></table><p>The flag is per-row, so a single subscription can have multiple <code>subscriptionPrices</code> schedules, each with its own <code>preserveCurrentPrice</code>. Apple uses the most recent one to decide what a new subscriber sees; existing subscribers stick to whatever schedule they signed up under, unless a <code>preserveCurrentPrice = false</code> row supersedes it.</p><p><strong><code>iap sync</code> deliberately writes <code>true</code></strong> — never accidentally migrate live subscribers. <code>iap migrate-prices</code> is the explicit opt-in for the <code>false</code> path.</p><h2 id="_3-subscription-price-points-are-per-territory" tabindex="-1">3. Subscription price points are per-territory <a class="header-anchor" href="#_3-subscription-price-points-are-per-territory" aria-label="Permalink to &quot;3. Subscription price points are per-territory&quot;">​</a></h2><p>Apple has a tier table (<code>subscriptionPricePoints</code>) and you set prices by referencing a tier ID — you can&#39;t pass a free-form <code>$4.99</code>. Each call to <code>findSubscriptionPricePoint(subId, territory, &quot;4.99&quot;)</code> returns the territory&#39;s tier ID matching that price.</p><p><strong>The CLI does this lookup for you.</strong> If your YAML says <code>base_price: &quot;4.99&quot;</code> in <code>base_territory: USA</code>, the CLI:</p><ol><li>Asks Apple for USA&#39;s tier table.</li><li>Finds the row with <code>customerPrice === &quot;4.99&quot;</code>.</li><li>Uses that row&#39;s ID.</li></ol><p>If no tier matches exactly, the CLI errors and lists the nearest tiers — Apple&#39;s table is discrete, so <code>$4.99</code> exists but <code>$4.97</code> doesn&#39;t.</p><h2 id="_4-the-anchor-migration-only-affects-the-anchor-s-territory" tabindex="-1">4. The anchor migration only affects the anchor&#39;s territory <a class="header-anchor" href="#_4-the-anchor-migration-only-affects-the-anchor-s-territory" aria-label="Permalink to &quot;4. The anchor migration only affects the anchor&#39;s territory&quot;">​</a></h2><p>Because each <code>subscriptionPrices</code> row is per-territory, <strong>migrating the anchor doesn&#39;t auto-migrate other territories</strong>. Apple&#39;s auto-equalised tier system handles the derived prices for <em>new</em> buyers in non-anchor territories, but existing buyers stay on whatever schedule they were signed up under in their territory.</p><p>The CLI&#39;s <code>iap migrate-prices</code> is <strong>anchor-only by default</strong>. Non-anchor territories are explicitly refused (clear error + hint to drop <code>--territory</code>) rather than silently using the wrong price-point ID — the point ID in the price summary is the anchor&#39;s, and reusing it for a different territory would migrate the wrong cohort.</p><p>Per-territory migration would need an extra lookup per scope; deferred until needed.</p><h2 id="_5-apple-picks-the-consent-flow-from-the-price-delta" tabindex="-1">5. Apple picks the consent flow from the price delta <a class="header-anchor" href="#_5-apple-picks-the-consent-flow-from-the-price-delta" aria-label="Permalink to &quot;5. Apple picks the consent flow from the price delta&quot;">​</a></h2><p>When <code>preserveCurrentPrice = false</code> triggers migration, <strong>Apple decides the customer-facing flow</strong> based on whether the new price is higher or lower than the old:</p><ul><li><strong>Price decrease</strong> → auto-applies at the subscriber&#39;s next billing date. No customer prompt.</li><li><strong>Price increase</strong> → Apple&#39;s standard customer notification + opt-in flow. Subscribers see a Settings prompt asking them to accept; declines cancel the subscription at the next renewal.</li></ul><p>You don&#39;t pass a &quot;consent type&quot; — Apple chooses. There&#39;s a separate <code>subscriptionPriceTimingType</code> enum for fine-grained control (e.g. <code>IMMEDIATELY</code> vs <code>WAITS_BILLING_CYCLE</code>), but the defaults work for the vast majority of cases.</p><h2 id="_6-iap-localisations-require-name" tabindex="-1">6. IAP localisations require <code>name</code> <a class="header-anchor" href="#_6-iap-localisations-require-name" aria-label="Permalink to &quot;6. IAP localisations require `name`&quot;">​</a></h2><p><code>InAppPurchaseLocalizationCreateRequest</code> requires <code>name</code> — passing only <code>description</code> errors out. The CLI ensures <code>display_name</code> is set on every locale; if you forget it in YAML, sync fails for that locale specifically (other locales still push).</p><p>This is different from listing localisations, where most fields are optional. Don&#39;t carry the same mental model across.</p><h2 id="_7-subscription-group-reference-name-is-immutable" tabindex="-1">7. Subscription-group reference name is immutable <a class="header-anchor" href="#_7-subscription-group-reference-name-is-immutable" aria-label="Permalink to &quot;7. Subscription-group reference name is immutable&quot;">​</a></h2><p>You can set <code>subscription_groups.&lt;key&gt;.reference_name</code> on creation, but it can&#39;t be changed afterwards. The CLI looks up existing groups by ASC ID, not by name, so you can rename in YAML without breaking the link — but the ASC UI keeps showing the original name.</p><p>If you really need to rename, delete the group on ASC (only possible if it has no active subscribers, which is almost never the case) and create a new one.</p><h2 id="_8-review-screenshots-are-required-for-submission" tabindex="-1">8. Review screenshots are required for submission <a class="header-anchor" href="#_8-review-screenshots-are-required-for-submission" aria-label="Permalink to &quot;8. Review screenshots are required for submission&quot;">​</a></h2><p>Apple Review needs a screenshot showing the IAP / subscription&#39;s purchase flow in your app. The CLI uploads it as part of <code>iap sync</code> when <code>review_screenshot: &lt;path&gt;</code> is set, but <strong>doesn&#39;t validate</strong> that the file is reasonable — it just uploads whatever you point at.</p><p>If you submit without a review screenshot, Apple rejects the IAP. The CLI surfaces upload errors but can&#39;t tell you whether the screenshot itself is good.</p><h2 id="_9-app-preview-videos-have-a-strict-spec" tabindex="-1">9. App Preview videos have a strict spec <a class="header-anchor" href="#_9-app-preview-videos-have-a-strict-spec" aria-label="Permalink to &quot;9. App Preview videos have a strict spec&quot;">​</a></h2><p>App Previews (per-locale promo reels) must match Apple&#39;s spec exactly:</p><ul><li><strong>Portrait iPhone tiers</strong>: 1080×1920, 30fps, AAC audio (silent is fine, but the track must exist).</li><li><strong>Landscape iPad tiers</strong>: their respective resolutions.</li><li>File must be H.264 in MOV/MP4 container.</li></ul><p>The CLI doesn&#39;t transcode — supply the right files. If Apple rejects the upload, the error usually says which dimension is wrong. Common gotchas:</p><ul><li>Native iOS screen recordings are 1284×2778 / 60fps / no audio — all wrong.</li><li>ffmpeg transcode incantation:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ffmpeg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> in.mov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -vf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;scale=1080:1920,fps=30&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">       -c:v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libx264</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pix_fmt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yuv420p</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -profile:v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> high</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -level</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4.0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">       -c:a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> aac</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -ar</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 48000</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -b:a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 64k</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -af</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;anullsrc&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -shortest</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">       -movflags</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +faststart</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> out.mov</span></span></code></pre></div></li></ul><h2 id="_10-worktree-vs-project-root" tabindex="-1">10. Worktree vs project root <a class="header-anchor" href="#_10-worktree-vs-project-root" aria-label="Permalink to &quot;10. Worktree vs project root&quot;">​</a></h2><p>The CLI distinguishes two roots:</p><ul><li><strong>Project root</strong> — the main repo. Where secrets live (<code>.secret-stuff/appstore-config.yaml</code>). Shared across worktrees.</li><li><strong>Worktree root</strong> — the current branch&#39;s working directory. Where metadata lives (<code>l10n/metadata/apple/iap.yaml</code>). Per-branch.</li></ul><p>This matters when you&#39;re editing IAP metadata on a feature branch in a worktree: the change stays in the worktree, but the secrets you use to push it come from the main repo. <code>git rev-parse --git-common-dir</code> discovers the main repo even from a worktree.</p><p>If you&#39;re not using worktrees, both roots are the same path — nothing to think about.</p><h2 id="_11-rate-limits-are-real-but-generous" tabindex="-1">11. Rate limits are real but generous <a class="header-anchor" href="#_11-rate-limits-are-real-but-generous" aria-label="Permalink to &quot;11. Rate limits are real but generous&quot;">​</a></h2><p>App Store Connect rate-limits aggressively for write operations, less so for reads. The CLI doesn&#39;t back off — if you hit a rate limit, the call fails with a clear error and you re-run. In practice:</p><ul><li><code>iap sync</code> for ~10 products including ~170 territories each: ~1 minute, no rate hits.</li><li><code>iap export</code> (full pull): ~90 seconds for the same setup.</li><li><code>listings update --all</code> for 14 locales: ~20 seconds.</li></ul><p>Bulk operations that fan out to many calls (e.g. screenshot uploads) are the most likely to hit limits. Spread big uploads across multiple runs if you see 429s.</p><h2 id="_12-lazy-sudoku-legacy-sku-prefix" tabindex="-1">12. <code>lazy_sudoku_*</code> legacy SKU prefix <a class="header-anchor" href="#_12-lazy-sudoku-legacy-sku-prefix" aria-label="Permalink to &quot;12. `lazy_sudoku_*` legacy SKU prefix&quot;">​</a></h2><p>(Lazy-Sudoku-specific, but illustrative.) When the project was set up, IAPs were named <code>lazy_sudoku_premium_lifetime</code> etc. Later renames dropped the prefix (<code>premium_lifetime</code>), but the old SKUs can&#39;t be renamed — Apple treats productId as immutable. If you&#39;re forking this CLI for your own project, <strong>choose your SKU names carefully on day one</strong>; renames mean creating new products + deprecating the old ones, not editing in place.</p>',49)])])}const g=t(a,[["render",o]]);export{u as __pageData,g as default};
